/**
 * Canonical protocol version. Bumped (semver) when the DecisionRequest/Response
 * contract changes. auto-claude #685 imports this exact value to detect drift.
 */
export const PROTOCOL_VERSION = "1.0.0";
